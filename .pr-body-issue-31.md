## Summary
- add a project-level `pyproject.toml` with Python 3.11 packaging metadata and grouped `runtime` / `bench` / `dev` extras
- document the supported environment and reproducible setup flow for runtime and benchmark work
- add packaging metadata regression coverage so the install story does not silently drift

## Implementation plan
1. Define the package/build metadata and group optional dependencies by use case.
2. Document the supported Python version and the runtime / benchmark environment setup explicitly.
3. Add a lightweight regression test that checks the packaging surface and install docs.
4. Verify the existing unit suite still passes with the new packaging/docs changes.

## Testing
- `python3 -m unittest tests.test_packaging_metadata -v`
- `python3 -m unittest discover -s tests -v`

Fixes #31.
